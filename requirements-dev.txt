# -------------------------------------------------------
# requirements-dev.txt
# Thư viện chỉ dùng cho Development & Testing
# KHÔNG cần cài trên Production
#
# Cài đặt: pip install -r requirements-dev.txt
# -------------------------------------------------------

# Load Testing
locust>=2.28.0

# Eval Pipeline (đã có httpx trong requirements.txt, không cần thêm)

# Unit Testing (nếu cần chạy pytest)
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0   # dùng chung với app, nhắc lại để rõ
